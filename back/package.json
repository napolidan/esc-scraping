{
  "name": "scrapping",
  "version": "1.0.0",
  "description": "",
  "main": "scrapers.js",
  "scripts": {
    "elena": "rm -rf build && cd ../front/ && npm run build && cp -r build ../back && cd ../back && node scrapers.js",
    "build:ui": "rm -rf build && cd ../front/ && npm run build && cp -r build ../back",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "puppeteer": "^20.4.0"
  }
}
